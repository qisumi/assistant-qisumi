# ===================================================================
# Assistant Qisumi - 环境变量配置示例文件
# ===================================================================
# 说明：复制此文件为 .env 并根据你的实际环境修改配置值
# 注意：.env 文件包含敏感信息，请勿提交到版本控制系统
# ===================================================================

# ------------------------------------------------------------------------
# 数据库配置 / Database Configuration
# ------------------------------------------------------------------------
# DB_TYPE: 数据库类型 (Database type)
# 可选值: sqlite, mysql
# - sqlite: 轻量级文件数据库，适合开发和测试
# - mysql: 生产环境推荐
DB_TYPE=sqlite

# DB_HOST: MySQL 数据库主机地址 (MySQL host)
# 仅在 DB_TYPE=mysql 时需要
DB_HOST=localhost

# DB_PORT: MySQL 数据库端口 (MySQL port)
# 默认值: 3306
DB_PORT=3306

# DB_USERNAME: MySQL 数据库用户名 (MySQL username)
# 仅在 DB_TYPE=mysql 时需要
DB_USERNAME=root

# DB_PASSWORD: MySQL 数据库密码 (MySQL password)
# 仅在 DB_TYPE=mysql 时需要
DB_PASSWORD=

# DB_DATABASE: MySQL 数据库名称 (MySQL database name)
# 仅在 DB_TYPE=mysql 时需要
DB_DATABASE=assistant_qisumi

# DB_FILE_PATH: SQLite 数据库文件路径 (SQLite database file path)
# 仅在 DB_TYPE=sqlite 时使用
# - 可使用绝对路径或相对路径
# - 默认值: ./assistant.db (可执行文件所在目录)
DB_FILE_PATH=assistant.db

# ------------------------------------------------------------------------
# HTTP 服务器配置 / HTTP Server Configuration
# ------------------------------------------------------------------------
# HTTP_HOST: 服务器监听地址 (Server bind address)
# - 0.0.0.0: 监听所有网络接口 (允许外部访问)
# - 127.0.0.1: 仅本地访问
# 默认值: 0.0.0.0
HTTP_HOST=0.0.0.0

# HTTP_PORT: 服务器监听端口 (Server port)
# 默认值: 4569
HTTP_PORT=4569

# ------------------------------------------------------------------------
# JWT 认证配置 / JWT Authentication Configuration
# ------------------------------------------------------------------------
# JWT_SECRET: JWT 签名密钥 (JWT signing secret)
# 安全提示: 生产环境请使用强随机字符串 (至少32位)
# 生成命令: openssl rand -base64 32
JWT_SECRET=dev-secret-key-for-jwt-authentication

# JWT_EXPIRE_HOUR: JWT 令牌过期时间（小时）(JWT token expiration in hours)
# 默认值: 24
JWT_EXPIRE_HOUR=24

# ------------------------------------------------------------------------
# 加密配置 / Encryption Configuration
# ------------------------------------------------------------------------
# API_KEY_ENCRYPTION_KEY: API密钥加密密钥 (Encryption key for stored API keys)
# 说明: 用于加密存储在数据库中的 LLM API Key
# 安全提示: 生产环境请使用强随机字符串 (必须32字节)
# 生成命令: openssl rand -hex 32
API_KEY_ENCRYPTION_KEY=dev-32-byte-encryption-key-for-api-keys

# ------------------------------------------------------------------------
# 日志配置 / Logging Configuration
# ------------------------------------------------------------------------
# LOG_LEVEL: 日志级别 (Log level)
# 可选值: debug, info, warn, error, fatal
# - debug: 调试信息（开发环境）
# - info: 一般信息（生产环境推荐）
# - warn: 警告信息
# - error: 错误信息
# - fatal: 致命错误
# 默认值: info
LOG_LEVEL=info

# ------------------------------------------------------------------------
# LLM 服务配置 / LLM Service Configuration
# ------------------------------------------------------------------------
# LLM_API_KEY: LLM 服务 API 密钥 (LLM provider API key)
# 获取方式:
# - 阿里云通义千问: https://dashscope.console.aliyun.com/
# - OpenAI: https://platform.openai.com/api-keys
# - 火山引擎豆包: https://console.volcengine.com/ark
LLM_API_KEY=

# LLM_MODEL_NAME: LLM 模型名称 (LLM model name)
# 常用模型:
# - qwen-plus: 阿里云通义千问 (推荐)
# - gpt-4: OpenAI GPT-4
# - doubao-seed-1-6-251015: 火山引擎豆包
LLM_MODEL_NAME=

# LLM_API_BASE_URL: LLM API 端点地址 (LLM API endpoint)
# 示例:
# - 阿里云: https://dashscope.aliyuncs.com/compatible-mode/v1
# - OpenAI: https://api.openai.com/v1
# - 火山引擎: https://ark.cn-beijing.volces.com/api/v3
LLM_API_BASE_URL=

# ------------------------------------------------------------------------
# LLM 深度思考配置 / LLM Thinking Configuration
# ------------------------------------------------------------------------
# LLM_THINKING_TYPE: 深度思考模式 (Deep thinking mode)
# 可选值:
# - disabled: 禁用深度思考能力
# - enabled: 强制使用深度思考能力
# - auto: 模型自行判断 (推荐)
# 默认值: auto
LLM_THINKING_TYPE=auto

# LLM_REASONING_EFFORT: 思考强度 (Reasoning effort level)
# 可选值:
# - minimal: 不思考 (最低成本)
# - low: 低强度思考 (轻度)
# - medium: 中等强度思考 (平衡模式，推荐)
# - high: 高强度思考 (深度思考)
# 默认值: medium
LLM_REASONING_EFFORT=medium

# ------------------------------------------------------------------------
# 助手配置 / Assistant Configuration
# ------------------------------------------------------------------------
# ASSISTANT_NAME: AI 助手名称 (AI assistant display name)
# 默认值: 小奇
ASSISTANT_NAME=小奇

# ------------------------------------------------------------------------
# 配置完成提示
# ------------------------------------------------------------------------
# 保存文件后，重启服务器使配置生效
# 运行命令: go run ./cmd/server
# ------------------------------------------------------------------------

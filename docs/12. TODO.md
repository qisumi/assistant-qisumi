1. 把 后端 handler 的 JSON 绑定 struct 写出来，比如 type CreateTaskFromTextResponse struct { Task TaskDTO; Session SessionDTO }，以及对应的 DTO <-> GORM 模型转换 的样板代码，这样整个 API 层会非常清晰、稳定。

2. **DependencyService 文档完善**: 详细说明 `internal/dependency/service.go` 中任务依赖关系的触发逻辑，包括：
   - `OnTaskOrStepDone()` 的调用时机
   - 依赖条件 (`task_done` / `step_done`) 与动作 (`unlock_step` / `set_task_todo` / `notify_only`) 的组合
   - 依赖链的传递执行

3. **部署方案文档**: 补充生产环境部署指南，包括：
   - Docker/Docker Compose 配置
   - Nginx 反向代理配置
   - 环境变量最佳实践
   - 数据库备份策略